# Dependencies & build output
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.vite/
.eslintcache

# Biến môi trường & bí mật — không bao giờ commit
.env
.env.*
!.env.example
!.env.prod.example
*.pem
*.key
*.jks
*.keystore
*.p8
*.p12

# Log
*.log
npm-debug.log*

# Editor / hệ điều hành
.vscode/
.idea/
*.local
.DS_Store
Thumbs.db

# Prisma
apps/api/prisma/migrations/dev.db

# Ảnh đại diện người dùng tự tải lên (apps/api/src/common/uploads-dir.ts) —
# dữ liệu runtime, không phải mã nguồn. Trên Docker được persist qua named
# volume api_uploads (xem docker-compose.yml/docker-compose.prod.yml).
apps/api/uploads/

# Dữ liệu nhân sự thật (GAHR26 chứa CCCD, ngày sinh, số điện thoại,
# địa chỉ, số tài khoản... của cán bộ) — tuyệt đối không đưa lên git
apps/web/Mau/**/gahr26*
apps/web/Mau/**/GAHR26*

# Audio giọng đọc thuyết minh — sinh bằng `npm run giong-doc` (apps/api/scripts/
# sinh-giong-doc.ts), có thể tái sinh lại từ DB bất cứ lúc nào nên KHÔNG cần
# theo dõi bằng git (~300MB+ toàn kho). Đưa lên PROD bằng cách chép thư mục qua
# USB, không phải "git pull" — xem DEPLOYMENT.md, Phụ lục E.
/assets/giong-doc/
